# Copy this file to .env (for docker compose) or .env.local (for `npm run dev`)
# and fill in real values. Never commit the filled-in version.

# Password for the hidden admin dashboard at /admin6k3-hgio
ADMIN_PASSWORD=
